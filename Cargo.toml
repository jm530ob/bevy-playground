[package]
name = "bevy-game"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = true

[dependencies]
bevy = { version = "0.15.1", features = ["wayland"] }
rand = "0.8.5"
